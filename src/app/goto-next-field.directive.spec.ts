import { GotoNextFieldDirective } from './goto-next-field.directive';

describe('GotoNextFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new GotoNextFieldDirective();
    expect(directive).toBeTruthy();
  });
});
